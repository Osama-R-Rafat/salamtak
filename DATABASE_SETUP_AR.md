# دليل الإعداد السريع لنظام سلامتك

## ⚡ البدء السريع

### 1️⃣ إنشاء حساب Supabase (مجاناً)

1. اذهب إلى: https://supabase.com
2. انقر على **Start your project**
3. سجل باستخدام حساب GitHub أو Google
4. انقر على **New Project**
5. املأ البيانات:
   - **Name**: salamtak-medical (أو أي اسم تختاره)
   - **Database Password**: اختر كلمة مرور قوية واحفظها
   - **Region**: اختر أقرب منطقة لك (مثل: Frankfurt للشرق الأوسط)
6. انقر على **Create new project**
7. انتظر 1-2 دقيقة حتى يتم إنشاء المشروع

### 2️⃣ إعداد قاعدة البيانات

1. من لوحة التحكم في Supabase، اذهب إلى **SQL Editor** (في القائمة الجانبية)
2. افتح ملف `database.sql` من المشروع
3. انسخ **كل** المحتوى
4. الصق المحتوى في SQL Editor في Supabase
5. اضغط على **Run** (أو Ctrl/Cmd + Enter)
6. انتظر حتى ترى رسالة "Success. No rows returned"

### 3️⃣ الحصول على مفاتيح API

1. في Supabase، اذهب إلى **Settings** (رمز الترس في الأسفل)
2. اختر **API** من القائمة
3. ستجد قيمتين مهمتين:
   - **Project URL**: يبدأ بـ `https://` وينتهي بـ `.supabase.co`
   - **anon public**: مفتاح طويل يبدأ بـ `eyJhbGci...`
4. احتفظ بهذه القيم (ستحتاجها في الخطوة التالية)

### 4️⃣ إعداد المشروع المحلي

1. افتح Terminal/CMD في مجلد المشروع

2. ثبّت المكتبات:
```bash
npm install
```

3. أنشئ ملف باسم `.env` في جذر المشروع

4. افتح الملف والصق هذا المحتوى (واستبدل القيم بقيمك الخاصة):
```
VITE_SUPABASE_URL=ضع_هنا_project_url_الخاص_بك
VITE_SUPABASE_ANON_KEY=ضع_هنا_anon_key_الخاص_بك
```

**مثال:**
```
VITE_SUPABASE_URL=https://abcdefghijk.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhY...
```

5. احفظ الملف

### 5️⃣ تشغيل المشروع

```bash
npm run dev
```

افتح المتصفح على: http://localhost:5173

## 🎉 اختبار النظام

### تسجيل طبيب جديد:

1. اختر "أنا طبيب"
2. انقر "ليس لديك حساب؟ سجل الآن"
3. املأ البيانات:
   - الاسم: د. أحمد محمد
   - البريد: ahmed@test.com
   - التخصص: اختر أي تخصص
   - رقم الترخيص: 12345
   - كلمة المرور: Test@123
4. اضغط "إنشاء الحساب"

### تسجيل مريض جديد:

1. اختر "أنا مريض"
2. انقر "ليس لديك حساب؟ سجل الآن"
3. املأ البيانات:
   - الاسم: محمد علي
   - الهاتف: 0123456789
   - البريد: mohamed@test.com
   - تاريخ الميلاد: 1990-01-01
   - كلمة المرور: Test@123
4. اضغط "تسجيل"

## ❓ حل المشاكل

### مشكلة: "Invalid Supabase URL"

**الحل:**
- تأكد من إنشاء ملف `.env` في المكان الصحيح (نفس مجلد `package.json`)
- تأكد من عدم وجود مسافات قبل أو بعد القيم في ملف `.env`
- أعد تشغيل المشروع بعد إنشاء ملف `.env`

### مشكلة: "new row violates row-level security policy"

**الحل:**
- تأكد من تنفيذ **كل** محتوى ملف `database.sql` في Supabase
- اذهب إلى SQL Editor وقم بتشغيل السكريبت مرة أخرى

### مشكلة: "Email already registered"

**الحل:**
- استخدم بريد إلكتروني مختلف
- أو قم بتسجيل الدخول بالبريد الموجود

## 🚀 نشر المشروع على الإنترنت (مجاناً)

### على Vercel:

1. ادفع المشروع إلى GitHub
2. اذهب إلى https://vercel.com
3. سجل دخول وانقر **Add New** → **Project**
4. اختر المشروع من GitHub
5. في **Environment Variables** أضف:
   - `VITE_SUPABASE_URL` = قيمة URL الخاصة بك
   - `VITE_SUPABASE_ANON_KEY` = قيمة المفتاح الخاص بك
6. انقر **Deploy**

### على Netlify:

1. ادفع المشروع إلى GitHub
2. اذهب إلى https://netlify.com
3. انقر **Add new site** → **Import an existing project**
4. اختر GitHub واختر المشروع
5. في **Build settings**:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. في **Environment variables** أضف نفس المتغيرات كما في Vercel
7. انقر **Deploy site**

## 📝 ملاحظات مهمة

- ✅ قاعدة البيانات على Supabase مجانية حتى 500 ميجابايت
- ✅ يمكنك دعوة مستخدمين غير محدودين
- ✅ جميع البيانات آمنة ومشفرة
- ⚠️ لا تشارك ملف `.env` مع أحد
- ⚠️ لا ترفع ملف `.env` على GitHub (هو محذوف تلقائياً)

## 🎊 مبروك!

الآن لديك نظام طبي كامل ومتصل بقاعدة بيانات حقيقية! 🎉

يمكنك:
- ✅ تسجيل أطباء ومرضى حقيقيين
- ✅ حجز مواعيد
- ✅ كتابة وصفات طبية
- ✅ إدارة السجلات الطبية
- ✅ كل البيانات محفوظة في قاعدة البيانات

---

**هل تحتاج مساعدة؟**
راجع ملف `SETUP_GUIDE.md` للحصول على تفاصيل أكثر.
